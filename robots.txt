# The private case studies must never be indexed. The noindex meta tag inside
# those pages is what actually enforces that; this line is a second layer.
User-agent: *
Disallow: /campaign-case-studies/

# Everything else: crawling is allowed so that crawlers can READ the noindex tag
# that is currently on every page. Blocking them here would stop them seeing it.
# To make the site findable later, remove the noindex meta tag from index.html
# and campaigns/index.html — do NOT touch the Disallow line above.
Allow: /

# AI training crawlers that honour robots.txt
User-agent: GPTBot
Disallow: /
User-agent: CCBot
Disallow: /
User-agent: Google-Extended
Disallow: /
User-agent: anthropic-ai
Disallow: /
User-agent: ClaudeBot
Disallow: /
